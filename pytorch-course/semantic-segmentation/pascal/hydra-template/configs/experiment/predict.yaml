# @package _global_

defaults:
  - override /data: pascal
  - override /model: pascal
  - override /trainer: gpu

tags: ["pascal", "fcn"]

seed: 36

model:
  net:
    model: "fcn"

# checkpoint path for the pre-trained model
ckpt_path: "logs/train/runs/2024-11-22_12-12-59/checkpoints/epoch_005_0.2214.ckpt"

data:
  data_dir: "./2007_000027.jpg"  # 데이터
