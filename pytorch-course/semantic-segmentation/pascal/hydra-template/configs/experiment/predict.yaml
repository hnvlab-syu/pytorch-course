# @package _global_

defaults:
  - override /data: pascal
  - override /model: pascal
  - override /trainer: gpu

tags: ["pascal", "fcn"]

seed: 36

model:
  net:
    model: "fcn"

# checkpoint path for the pre-trained model
ckpt_path: "logs/train/runs/2024-11-21_20-14-10/checkpoints/epoch_001_0.1393.ckpt"

data:
  data_dir: "./2007_000027.jpg"
